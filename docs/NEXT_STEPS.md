# 下一步行动计划（P4 之后）

> 日期：2026-09-16　起点：`9815961`（P0–P4b 已完成）
> 缺陷编号（F1–F7）沿用 [`CODE_REVIEW_P4.md`](CODE_REVIEW_P4.md) §3；优化依据见 [`OPTIMIZATION_P4.md`](OPTIMIZATION_P4.md)。

---

## 0. 先纠正两个事实（规划前必须对齐）

| 项 | 实际情况 | 影响 |
|---|---|---|
| 未推送提交 | **7 个**（`aca63fd` P0+P1 → `205f5fa` P2 → `5df4216` P3 → `d869660` P4a → `0804c5d` P4b → `525847e` 复核 → `9815961` 优化），远端仍停在 `865048f` | 一整个 P0–P4b 只在本地 |
| 桌面 EXE | **不存在**（仓库根也无副本）；仅剩 `installer/dist/AI_NovelWriter.exe`，构建于 `62bb854`，**落后 HEAD 10 个提交** | 你现在手上**没有**含 P2–P4b 能力的可执行文件 |

⇒ 前两项（S0 / S3）因此被提到最前面：它们不是"优化"，而是"让已完成的工作真正可用"。

---

## 0.5 执行情况（2026-09-16 更新）

| # | 状态 | 提交 | 实际结果 |
|---|---|---|---|
| **S0** | ✅ 完成 | 用户执行 | 远端 = 本地 |
| **S1** | ✅ 完成 | `94a8d83` | 121 文件重排格式；`ruff format --check` **143/143 达标**且 CI 该步**转为阻断**；用 **AST 等价性**证明零语义变更（143 文件 AST 全同，仅 4 文件 14 处 docstring 的尾随空格变化，**无 prompt 模板受影响**） |
| **S2** | ✅ 完成 | `0b5b817`+`6c1767f` | `chapter_count` 286 → **1094**（真章数）；原值保留为 `character_file_count`；`--verify` 5 项全通过；`characters_sha256` **未变**；`--record` 已幂等；新增 `--no-tests` |
| **S3** | ✅ 完成 | 未入库（产物） | 重建 EXE：**20,083,173 B**，sha256 `01D5419F…5C0A0662`；构建 TOC **含全部 9 个新模块**；`warn-novel_app.txt` 中**项目内模块缺失 = 0**；已分发到桌面 + 仓库根（`*.exe` 已 gitignore） |
| **S4** | ✅ 完成 | `488a31e` | F3：`add_event` 采集轨迹（**人物轨迹终于有真实数据**，一批角色只落盘一次、失败不影响事件写入）；F4：传记纯逻辑收进 `app/biography.py`，面板再导出（`is` 同一对象），`character_ui` 入口改用同一提示词 + 同一四类素材 + 同时写结构化 JSON |
| **S5** | ✅ 完成（机制+棘轮，P5 第一步） | `59338ea` | `UIStyle.FONT_ROLES` + `font(role)`；**我自己拥有的 4 个文件归零**（零视觉变化已证）；`test_font_token_ratchet.py` 棘轮（**385 处/25 文件**只减不增）；顺带修老面板 `memory is None` 判空、放宽颜色守卫判据；**修掉我引入的缓存边界**（等长重写漏检） |
| **S6** | ✅ 完成 | `127cbf1` | 分支子项目**可打开 + 进代际树**：`meta.json` 补 `title`/`lineage`（分支与父代**同代**、仍 `readonly_parent`）；`discover_branches` 统一读取器；代际树把分支挂在所属作品下并可双击打开；`BasePanel.open_novel_dir` 走宿主入口；**顺带修掉"面板导入失败静默"**（`registry.LOAD_FAILURES` → 落磁盘诊断日志） |
| **S7** | ✅ 完成 | 见提交 | **面板 UI/UX 系统化改造**（用户反馈"简陋、操作不便"）—— 详见下方 §0.6；**第二轮（可读截图后）又抓到 4 个真 bug**（重复渲染 / KPI 永远占位 / 换肤掉皮 / 米色控件），见 §0.7 |

---

## 0.6 面板 UI/UX 改造（S7）—— 改进方向与实测对照

### 先测再改：问题到底在哪

用 `tests/_ui_metrics.py`（可复现）对 15 个面板做量化体检，**推翻了两个想当然的判断**：

| 度量 | 改造前实测 | 结论 |
|---|---|---|
| `text_muted` 在 `bg_card` 上的对比度 | **2.68:1**（标准 ≥4.5） | 🔴 **真根因**：全应用提示文字都发灰难认 |
| 点击目标（按钮/输入框高度） | **0 处过小** | ✅ 原本就合格（我原以为太小的按钮，测下来没问题） |
| 间距取值种类 | 每面板 2–4 种 | ✅ 比预想的好（7–8 种是跨文件累计，不是单面板） |
| **面板控件数** | 记忆可视化 **6 个**、风格转换 9、摘要管理 11 | 🔴 **真问题**：面板"太薄"，只有"一个按钮 + 一个文本框"，谈不上信息结构 |
| `messagebox` 调用 | 全仓 **64 处** | 🔴 每次提示都打断操作流 |
| 键盘绑定 | 全仓 **5 个** | 🔴 只能靠鼠标，重复操作成本高 |
| Treeview 主题 | **未配置**（clam 默认灰） | 🔴 列表与暗色皮肤冲突 |

### 六个改进方向（都已落地）

1. **调色板先修**（一处改动改善全部 15 个面板）：`text_muted` 提亮到 4.54:1；
   新增 `accent_text / success_text / info_text / error_text / warning_text` 文字变体，
   并改掉 10 处"把填充色当文字色"的用法（`accent` 只有 2.75:1）。
2. **一套组件库**（`app/panels/ui_kit.py`）：卡片 / 工具栏 / 搜索框 / 空态 / 状态栏 /
   轻提示 / 可滚动容器 / KPI 小块 / 徽标 / 表格 / 快捷键 —— 面板只管"放什么"，
   "长什么样、怎么交互"由库统一。
3. **宿主统一外壳**（`PanelHost._build_with_chrome`）：每个面板自动获得
   **面包屑（分组 › 面板名）+ 刷新按钮 + 底部状态栏**，以及 **F5 / Ctrl+F / Esc** 快捷键 ——
   一处实现、15 个面板同时生效（含 12 个 v2 迁移面板）。
4. **表格统一**（`Panel.Treeview`）：28px 行高（默认 20 对中文太挤）、斑马纹、
   表头加粗、选中高亮、**点表头排序**、**Enter 等效双击**。原本 7 个表格全是 clam 默认灰。
5. **内联反馈**：新增 `StatusBar` 与 `toast()`（轻提示浮窗，不抢焦点、自动消失），
   面板"操作已成功/失败"不再必须弹窗打断。`messagebox` 只保留**需要用户确认**的场景。
6. **迁移面板润色**（`ui_kit.polish_legacy`）：对 12 个 v2 面板已构建的控件树做事后统一 ——
   按钮手型光标/悬停/点击高度、输入框聚焦边框、LabelFrame 配色、表格套样式；
   **只改外观、不动布局**（有测试守住 `pack_info` 不变）。

### 改造后实测

| 度量 | 改造前 | 改造后 |
|---|---|---|
| 最差文字对比度 | 2.68:1 | **4.51:1** |
| 面板对比度违规 | 3 个面板各有 1 处 | **15/15 全为 0** |
| 统一外壳（面包屑+刷新+状态栏） | 无 | **15/15** |
| 表格统一样式（28px + 斑马纹） | 0/7 | **7/7** |
| F5 刷新绑定 | 0 | **15/15** |
| 字号种类 ≤4 | 1–4 | 1–4 |

门禁：`tests/test_panel_ui_quality.py`（29 条）把上述可测项写成断言，防止改一次退一次。

---

## 0.7 第二轮：把"截图读进来"之后抓到的 4 个真 bug（同一提交）

用户提示"尝试读取上一轮的截图后再调整"。**上一轮我读不了图**，只能靠度量指标；
这次截图可读，一眼就看出度量体系**根本没覆盖**的问题 —— 这 4 个全是"看得见、测不到"的类型：

| # | 截图暴露的现象 | 根因 | 修法 |
|---|---|---|---|
| 1 | 元素库面板**渲染了两份**（两组面包屑+两组面板内容） | `refresh()` 先把 `is_built` 置 False 再 `select()`，而外壳与面板内容长在**同一个 frame**；不销毁旧控件就再建一遍 ⇒ 打开/切换小说（`novel.opened` → `refresh()`）或**按 F5 必现** | `select()` 重建前先 `destroy` 该 frame 的全部子控件（顺带自愈"上次构建中途异常留下的半个外壳"） |
| 2 | KPI 卡片永远显示占位符「—」（旁边摘要行却是有数字的） | 靠 `cget("font") == UIStyle.font("title")` 找数值 Label；而 `cget("font")` 返回 **Tcl 字体名（字符串）**，与元组**永不相等** | `kpi_row` 直接把数值 Label 列表交出来（`value_labels`），不再靠字体比对定位 |
| 3 | 切走再切回迁移面板，**深色换肤"掉皮"**（Listbox 又变回 `SystemWindow` 白底） | 迁移面板 `on_show()` 会**重建内容**（v2 语义），新控件回到系统默认色，而润色只在**首次构建**时跑过一次 | `on_show` 之后为迁移面板**重跑** `polish_legacy`（`test_polish_is_repeatable_after_rebuild` 钉住） |
| 4 | 元素库/网搜/摘要管理里大片**米色横带与米色列表** | ① `tk.Listbox` 没设颜色（系统默认 `SystemWindow`）；② v2 面板的 `ttk.Frame/Label/Button` **不指定 style** → 退回 clam 默认浅色；③ `ScrolledText` 自带的 `tk.Scrollbar` 也是米色 | 润色补齐 `tk.Listbox` / `tk.Scrollbar` / `tk.Label` / `tk.Frame`，以及**未指定 style 的 ttk 控件**映射到 `Dark.TFrame/Dark.TLabel/Secondary.TButton/Dark.VScrollbar` 等；Combobox 弹出列表用 option 数据库着色 |

### 一个值得记下的 Tk 陷阱

`ttk.Combobox` 继承链是 `Combobox → ttk.Entry → tk.Entry`（为复用 validate 机制）。
所以润色里的 **ttk 分支必须排在 tk 分支前面** —— 否则下拉框被"Entry 分支"截胡，
`style="Dark.TCombobox"` 永远轮不到执行（本轮实测：`touched` 计数正常但仍未换肤，
排查花了三个探针脚本）。`test_ttk_widgets_are_not_shadowed_by_tk_branches` 钉住这条顺序。

### 判据（避免"误伤有意的配色"）

"该不该换肤"的判据不是猜，而是 **`cget("bg")` 是否为系统色名**：
未配色的控件报告 `SystemButtonFace` / `SystemWindow`，显式配过色的报告 `#rrggbb`。
因此"没配色 → 换；配过色 → 不动"有明确可判定的依据（`test_explicitly_colored_widgets_are_left_alone`）。

### 第二轮后的验收

| 项 | 结果 |
|---|---|
| 面板外壳唯一性（首次 / refresh 后 / 连续 refresh / 切走切回） | **每种情况都恰好 1 份** |
| KPI 数值 | 随 `snapshot.stats` 实时更新（`4 / 4 / 3 / 1 / 1`） |
| 迁移面板切回后 | 与首次构建**完全一致的深色**（不再掉皮） |
| 门禁用例 | `test_panel_ui_quality.py` 由 29 → **35 条**，全绿 |
| 回归 | 面板相关 456 + 基础/约束 162 全绿；`ruff check` 全绿、`ruff format` 全达标 |

**仍未解决（诚实记录）**：`chapter_analysis` 面板在离屏演示环境里 `select()` 返回 `False`
（构建抛错，与本次改动无关，第一轮截图同样缺这一张）—— 需要在**真实应用**里点开该面板取证。

**P5 剩余部分**（不在本轮）：`app/` 下 **385 处**硬编码字体元组的角色化命名与替换（含 55 处计算式）、
`dialogs.py` 抽取、面板"脱离为独立 Toplevel"。棘轮已就位，可增量推进；清零后把
`TestFontRatchet` 的"原生面板为 0"收紧为"全仓为 0"。

### 如何在打包后的 EXE 里确认"面板到底有几个"

界面是按分组显示选择器的，不容易一眼数出个数。可靠的核对方式有两条：

1. **看创作工具页**：选择器共 **5 行**，第 4 行是`世界与世代`，含
   `世界线与时间线`、`角色传记`、`世代传承` 三个（第 1 行`创作素材` 7 个，
   第 2 行`结构分析` 2 个，第 3 行`记忆与摘要` 2 个，第 5 行`运维` 1 个 —— 合计 15）。
2. **查磁盘日志**（2026-09-16 起）：启动时会往
   `~/.ai_novel_writer/diagnostic_logs/diagnostic-<日期>.jsonl` 写一条
   `event="panel_registry"`，里面有 `total`、`by_category`、`native`、`legacy`，
   以及 **`load_failures`**（正常为空；非空即说明某个面板模块在冻结环境里导入失败）。
   打包成 windowed EXE 后没有控制台，此前这类失败**完全不可见**，现在可事后核对。

**本轮另一个收获**：测试基础设施被连续踩到第 3、4 类脆弱形态，已在 `tests/_source_scan.py` 收口 ——
`count_normalized`（对格式化换行免疫）与 `strip_noise` 改用 `tokenize` 去注释
（此前 `bg="#101020", fg=C["typo"])` 会被 `#` 截断，使**颜色守卫静默漏检**）。

---

## 1. 建议总览（按优先级）

| # | 建议 | 优先级 | 预期目标 | 主要风险 / 注意事项 |
|---|---|---|---|---|
| **S0** | 推送 7 个提交；处理凭据两项待办 | 🔴 立即 | 远端 = 本地；token 风险闭环 | token **2026-10-16 到期**（约 30 天）；fine-grained token 仍具 `admin`、旧 classic PAT 未吊销 —— **两项都只能你在网页操作** |
| **S1** | 全量 `ruff format` 一次，并把该门禁转为阻断（F5） | 🔴 高 | 格式从"噪声"变成"门禁"；为 S5 铺路 | 触及 **122 个文件**；必须**单独提交**、零语义变更；`mypy` 存量 ~100 条**不要同一提交里一起改** |
| **S2** | 修 F1：基线 `chapter_count` 名副其实 | 🟠 中高 | P0 护栏恢复"章"维度，能发现章节被删 | `--verify` 会报 286→1094，需 `--record` 一次并在提交信息里说明**这是修正、不是回归** |
| **S3** | 重新打包 EXE 并交付 | 🟠 中高 | 你手上有含 P2–P4b 的可用程序 | onefile 解包变慢；先跑启动冒烟再交付；桌面 + 仓库根双份 |
| **S4** | F3 接线 `update_character_activity` + F4 收敛传记入口 | 🟡 中 | 人物轨迹有真实数据；同功能不再两种质量 | F3 在成章路径上新增写盘 ⇒ 必须 try/except 且**不得阻断生成**；F4 保留原入口、内部委托 |
| **S5** | **P5 样式与对话框收敛**（主任务） | 🟡 中 | 字体令牌化 + `dialogs.py` + 面板 detach；验收：无字面 `font=` 断言 | **必须先做 S1**；不要把"样式"与"布局"混在同一提交；顺带补老面板判空与 F6 |
| **S6** | F2：`branch_%03d/` 是否做成可打开的子项目 | ⚪ 决策 | 把"只写不读"的能力接上 | 产品决策：分支作品算不算独立作品？若做，需定它是否进 `novels_dir`、是否进代际树 |

---

## 2. 各项展开

### S0 推送与凭据闭环 —— 🔴 立即（约 5 分钟）

- **动作**：在你本机仓库目录执行 `git push origin main`（本地已有 7 个提交待发）。
  ⚠️ 注意 `git push` 在本沙箱可用但凭据 helper 路径受限制，**由你在真实终端执行最稳妥**。
- **同时处理（只能你操作）**：
  1. fine-grained PAT 收紧为「仅 `AI_NovelWriter` + `Contents: Read and write`」，去掉 `Administration` 与另两个仓库；
  2. 吊销旧 classic PAT（18 个 scope 含 `admin:org`，且能绕过 PR 保护）。
- **风险**：token **2026-10-16 到期**。若 S0 拖过该日期，推送会直接失败且报错方向容易被误判为网络问题。
- **顺序**：先生成新 token → 再吊销旧 token → 最后更新凭据管理器（否则推送中断）。

### S1 格式基线（F5）—— 🔴 高

- **为什么排第二**：P5 的验收标准是「**无字面 `font=` 断言**」。在 122 个文件待格式化的现状下，
  这条断言会被格式噪声淹没，无法判断真假。
- **动作**：`ruff format app/ tests/` → **单独提交**（纯格式）→ 把 CI 里 `ruff format --check`
  的 `continue-on-error: true` 去掉。
- **注意**：
  - 提交前记录测试数（当前 **2173**）与 `ruff check` 结果，提交后必须**完全一致**；
  - `pyproject.toml` 注释指出 `W291/W293` 的剩余项位于**多行字符串**内 —— 格式化**不应**改变字符串内容，
    但仍建议对 `git diff` 做一次「是否触及引号内文本」的抽查（尤其 `prompt` 模板）；
  - `mypy` 存量 ~100 条错误**不要**在同一提交里处理（会让 diff 无法审阅）。它应作为独立批次逐步收敛或加 `exclude`。

### S2 修 F1（基线章维度）—— 🟠 中高

- **问题**：`live_data.summarize_novel` 里 `chapter_count=_count_json_files(novel_dir/"characters")` ——
  数的是**角色文件数**；`format_text()` 却打印成 `chapters`（`baseline_check.py:174` 老实标为"角色文件数"）。
  真章数（线上 **1094**）从未被采集 ⇒ **`--verify` 无法发现章节被删**。
- **动作**：`chapter_count` 改为统计 `chapters/*.txt`；原值改名为 `character_file_count` 保留（别静默丢掉这个数据点）；
  同步修正 `format_text()` 的标签。
- **风险**：`baseline_check --verify` 会立刻报 286→1094 的差异。这是**修正后的正确差异**，
  需在改完后 `--record` 一次，并在提交信息里写清楚，否则下次看历史的人会误以为是数据事故。
- **验证**：对真实线上小说跑一次 `--record` + `--verify`，确认 `characters_sha256` 仍为
  `FDD2D44D…DB306A21`（改的只是计数维度，**角色数据不得有任何写入**）。

### S3 重新打包 EXE —— 🟠 中高

- **现状**：桌面无 EXE；`installer/dist/AI_NovelWriter.exe` 构建于 `62bb854`，**落后 10 个提交**
  ⇒ 其中**没有** Provider 注册表（P2）、用量面板（P3）、面板框架与三个新面板（P4a/P4b）、以及复核与优化。
- **动作**：`cd installer && <Python311> -m PyInstaller novel_app.spec --clean --noconfirm`，然后跑启动冒烟并分发到桌面 + 仓库根。
  （`novel_app.spec` 的 `hiddenimports` 已在 P4a/P4b 补齐，无需再改。）
- **验收（缺一不可）**：
  - onefile 呈**双进程**结构（引导器 ~10MB + 真实应用 ~60MB）；
  - `MainWindowTitle` 非空（应为 `AI小说创作工坊 v2.16.0`）；
  - **创作工具页能看到 15 个面板**（12 迁移 + 世界线与世代 3 个）；
  - 跑一次生成后 `usage.jsonl` 有该章记录。
- **风险**：onefile 首次启动解包较慢（35MB 解包）；若杀软拦截，改用 onedir 版本。
- **建议节奏**：S3 先出一版（让你能实际点新面板、给反馈），P5 完成后再出第二版。

### S4 接线与收敛 —— 🟡 中

- **F3**：现在 `update_character_activity` **没有任何生产调用方** ⇒ `character_activity.json` 实际为空，
  "人物轨迹泳道"靠事件源兜底才有数据。在成章成功后补一次调用即可让"最近出现/重要度"变成真实数据。
  - ⚠️ 它在**生成热路径**上新增写盘：必须 `try/except` 包裹、失败只记日志；
  - 它内部已有 100 条上限与批量落盘，不要绕过它直接写文件。
- **F4**：`character_ui._generate_character_biography` 与新传记面板两套逻辑并存，
  且前者的提示词**只用 `outline[:5]`、完全不用已写正文** ⇒ 同一功能两种质量。
  - 建议：**保留原入口**，内部委托到新面板用的提示词构造（`build_biography_prompt`），
    这样老用户路径不变、质量统一；不要删入口（会让用户以为功能消失）。

### S5 P5 样式与对话框收敛（主任务）—— 🟡 中

- **范围**（ROADMAP §5）：A4 / A5 / A7 余下 + 面板 detach；字体令牌化、`create_styled_*` 接线、
  `dialogs.py`、独立窗口。
- **验收**：无字面 `font=` 断言；UI 冒烟通过。
- **前置**：**S1 必须先完成**。
- **风险与对策**：
  - 12 个老面板的视觉回归 → 用「令牌 + 断言 + 真实 Tk 冒烟」三重手段；
  - **不要顺手改布局** —— 样式与布局混在一个提交里，出问题无法定向回滚；
  - 顺手补两处小缺口：
    - `memory_viz` / `summary_mgmt` 不防 `self.memory is None`（宿主已捕获，但面板自身应判空）；
    - F6：颜色令牌守卫只覆盖含字面 `C = UIStyle.COLORS` 的文件，判据放宽为「出现 `UIStyle.COLORS` 或 `COLORS[`」。

### S6 F2 分支子项目 —— ⚪ 决策项（不阻塞其他）

`timeline_ui` 会建出 `timelines/branch_%03d/`（含完整目录树），但**全仓无读取方**；本轮已把它列进
时间线面板让"写了能看见"。是否进一步做成「可打开的子项目」是产品决策：
- 若做：需定义它是否算独立作品（进 `novels_dir`？进代际树？与 `meta.lineage` 的关系）；
- 若不做：建议至少在 UI 上说明它是"草稿分支，仅供查看"。

---

## 3. 推荐的执行顺序

```
S0 推送 + 凭据闭环        （立即，5 分钟 —— 让 7 个提交与你的机器对齐）
   ↓
S1 格式基线（单独提交）   （半天 —— P5 的前置条件）
   ↓
S2 修 F1 基线章维度       （半天 —— 恢复数据护栏的一整个维度）
   ↓
S3 打包 EXE 交付          （半天 —— 让你能实际使用 P2–P4b 的全部能力）
   ↓
S4 F3 接线 + F4 收敛      （1 天 —— 小改动、直接见效）
   ↓
S5 P5 样式与对话框收敛    （2–3 天 —— 主任务）
   ↓
S6 F2 分支子项目          （决策后另排）
```

**若只有半天**：只做 **S0 + S1 + S3** —— 推送、格式门禁、拿到可用的 EXE。
这三件完成后，剩下的都是"锦上添花"，且此时你已经有实物可点、可反馈。

---

## 4. 每项的验收口径（避免"做完了但说不清做到了没"）

| # | 验收 |
|---|---|
| S0 | `git ls-remote origin main` == 本地 HEAD；GitHub 上该仓库 `permissions.admin == false` |
| S1 | `ruff check` + `ruff format --check` 均全绿（无 `continue-on-error`）；测试数与结果与改前逐项一致 |
| S2 | `scripts/baseline_check.py --verify` 通过；`chapter_count == 1094`（不再等于角色数）；`characters_sha256` 未变 |
| S3 | 双进程结构 + 标题非空 + **15 个面板可见** + `usage.jsonl` 有记录 |
| S4 | 生成一章后 `character_activity.json` 出现该角色记录；传记从两个入口生成的质量一致 |
| S5 | 全仓无字面 `font=`；`dialogs.py` 存在且被引用；面板 `detach` 有测试覆盖；UI 冒烟通过 |
